# Unity
# Indicamos que ignore la carpeta Library, que contiene archivos generados automáticamente.
# Es seguro ignorarla y se regenerará cuando se abra el proyecto.
Library/
# Indicamos que ignore la carpeta Temp, que contiene archivos temporales de compilación y caché.
Temp/
# Indicamos que ignore la carpeta Obj, que contiene archivos objeto intermedios de compilación.
Obj/
# Indicamos que ignore la carpeta Builds, comúnmente usada para guardar los ejecutables del juego.
Builds/
# Indicamos que ignore la carpeta Logs, que contiene registros de ejecución.
Logs/

# Autogenerated VS/MD/Eclipse projects
# Ignora archivos de solución y proyecto generados por IDEs como Visual Studio o MonoDevelop.
*.csproj
*.unityproj
*.sln
*.suo
*.munin
*.opensdf
*.sdf
*.user
*.userprefs
*.pidb
*.booproj
*.svd
*.asmdef.meta
# Editor-specific files
.vs/
.vscode/

# Build results
# Ignora los archivos ejecutables y de datos generados al compilar el juego para Windows/Mac/Linux.
*.apk
*.aab
*.unitypackage
*.app
*.exe
*.win
*.mac
*.linux
*.json

# Memory Captures
# Ignora archivos grandes de captura de memoria.
*.snap

# Unity Crash Handler
# Ignora archivos de reportes de errores.
**/crash_handler.override

# Ensure meta files are tracked for everything else
# Esta línea asegura que, aunque la carpeta Library/ esté ignorada, sus archivos meta NO lo estén.
!Library/*.meta